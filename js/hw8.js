const glueTogetherWords = function () {
    const firstWord = 'Hello';
    const secondWord = 'World';
    const togetherWords = ` ${firstWord} ${secondWord}`;

    return(togetherWords);
}
console.log(glueTogetherWords());